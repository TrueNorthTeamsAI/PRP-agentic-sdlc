{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if command -v pwsh >/dev/null 2>&1; then pwsh -File \"${CLAUDE_PLUGIN_ROOT}/hooks/prp-ralph-stop.ps1\"; elif command -v powershell >/dev/null 2>&1; then powershell -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/hooks/prp-ralph-stop.ps1\"; else bash \"${CLAUDE_PLUGIN_ROOT}/hooks/prp-ralph-stop.sh\"; fi"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "if command -v pwsh >/dev/null 2>&1; then pwsh -File \"${CLAUDE_PLUGIN_ROOT}/hooks/prp-research-team-stop.ps1\"; elif command -v powershell >/dev/null 2>&1; then powershell -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/hooks/prp-research-team-stop.ps1\"; else bash \"${CLAUDE_PLUGIN_ROOT}/hooks/prp-research-team-stop.sh\"; fi"
          }
        ]
      }
    ]
  }
}
