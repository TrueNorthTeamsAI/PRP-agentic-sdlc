---
name: version
description: Display the current prp-core plugin version
user-invocable: true
---

Read the file `plugins/prp-core/.claude-plugin/plugin.json` (resolve relative to the repository root where the plugin is installed).

Output the version in this format:

```
prp-core v{version}
```

Nothing else.
